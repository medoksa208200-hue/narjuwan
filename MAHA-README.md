# إضافة مشروع المها

تم تحديث قاعدة Supabase الحالية وإضافة 967 قطعة: 930 سكني و37 تجاري، مع بقاء 142 وحدة نرجوان.

## تشغيل الواجهة على موقعك الحالي

ارفع محتويات هذه الحزمة إلى مستودع الموقع الحالي، وبالأخص `index.html` و`efragh/index.html`. إعدادات Cloudflare Pages الحالية لا تتغير: بدون أمر بناء، والمخرجات من جذر المستودع. تحديثات قاعدة البيانات الموجودة في `database` طُبقت بالفعل؛ لا تحتاج إعادة تشغيلها.

## السلوك

- اختيار المها يُظهر قائمة الاستخدام: كل الاستخدامات، سكني، تجاري. التصنيف ثابت لكل قطعة من الإكسيل، وليس سعرين قابلين للتبديل لنفس القطعة.
- اختيار البلوك ثم القطعة. تعرض الأرض المساحة والزون وعدد الشوارع وعرض كل شارع بالمتر حسب اتجاهه.
- بيانات الشقق تظل في نرجوان، وتُستبدل في مستندات المها ببيانات الأرض والشوارع.
- سعر الأرض هو قيمة العقار قبل الرسوم. السعي وضريبته والتصرفات تُحسب مرة واحدة عند إصدار المستند.
- الاستخدام ونوع الأصل للأرض يُفرضان من بيانات القطعة في الخادم. اللقطة المحفوظة تشمل بيانات الشوارع.
- نسبة السعي المختارة تُحتسب في الخادم أيضًا، مع الإبقاء على المستندات القديمة.
- المساحة تحتفظ بالكسور. مبالغ المستند تُعرض مقربة للريال، وفق سلوك النظام الأصلي.
- QR يشير إلى الاستعلام عن القطعة على نفس نطاق الموقع.

## مصدر البيانات والاستثناءات

اعتمدنا ورقتي «تجاري» و«سكني»، وسعر كل صف التفصيلي. تم التحقق من المساحة × سعر المتر لكل القطع الـ967.
لم تُستورد «ورقة1» لأنها تحمل مخططًا مختلفًا برقم 3980.
لم تُستورد القطع 968–973: ثلاثة مساجد وخدماتها، مرفقان تعليميان، ومرفق صحي، لعدم وجود أسعار وعدم وضوح بلوكاتها في هذه الصفوف. الصفوف الأصلية محفوظة في `database/excluded-facilities.json` للمراجعة.
الشوارع الفارغة تظل غير محددة، ولا يُفترض لها عرض أو اتجاه.

## التحقق

شغّل `node tests/maha.test.cjs` للتحقق من البيانات، والحسابات، ولقطة الأرض، وحقول المستند، وصحة JavaScript.
تم أيضًا التحقق من الأعداد والقراءة العامة وصلاحية رفض إصدار مستند بلا جلسة على Supabase.
لم يُختبر إصدار مستند فعلي بحساب موظف، ولم يُجر اختبار بصري للطباعة داخل متصفح.
تحذيرات تدقيق Supabase الخاصة بدوال SECURITY DEFINER موجودة ضمن نظام الجلسات المخصص الحالي؛ الدوال المعدلة حافظت على التحقق من جلسة الموظف. مرجع: https://supabase.com/docs/guides/database/database-linter?lint=0028_anon_security_definer_function_executable
